default:
  suites:
    default:
      contexts:
        - FeatureContext
  extensions:
    Behat\MinkExtension:
      base_url: http://localhost:8000
      #Goutte c'est un navigateur comme quand on utilise un driver avec Selenium/Phyton3
      # ~ c'est pour avoir les valeur par default du driver Goutte
      goutte: ~
      